spring:
  profiles:
    active:
    - dev
---
spring:
  profiles: dev
  application:
    name: config-dev
    
eureka:
  client:
    service-url:
      defaultZone: http://eureka7001.com:7001/eureka
---
spring:
  profiles:
    active:
    - test
---
spring:
  profiles: test
  application:
    name: config-test
 
eureka:
  client:
    service-url:
      defaultZone: http://eureka7002.com:7002/eureka
